# Fortschritt 2026-03-23

## Umgesetzte Aenderungen

- Windows-Erkennung und Start von OpenClaw robuster gemacht
- Session-Status fuer OpenClaw-Pfad korrigiert
- Assistant-/WhatsApp-MVP-Route in die App integriert
- UI-Texte bereinigt und Restart-Skript hinzugefuegt
- Schutzstatus in Netzwerk, Prozesse und Dateisystem getrennt dargestellt
- AppContainer-basierte Dateisystem-Haertung in `src-tauri` aufgebaut
- Dashboard und Session-Ansicht so angepasst, dass fehlendes OpenClaw klarer angezeigt wird
- Gespeicherte OpenClaw-Pfade werden jetzt validiert, bevor sie im Status verwendet werden

## Aktueller Stand

- OpenClaw wurde bewusst global deinstalliert, damit der Installationsflow erneut sauber getestet werden kann
- CrabCage zeigt fehlende OpenClaw-Installation jetzt deutlicher an
- Die App ist baubar, und die Desktop-Bundles werden erfolgreich erzeugt

## Offener technischer Punkt

- Der AppContainer-Selbsttest fuer `node.exe -v` scheitert weiterhin unter Windows mit `os error 2`
- Das Problem liegt aktuell im nativen AppContainer-Startpfad, nicht mehr in der OpenClaw-CLI selbst

## Sinnvoller naechster Schritt

- OpenClaw aus CrabCage erneut installieren
- Danach den Session-Start ohne Altlasten erneut pruefen
- Falls der AppContainer-Fehler bleibt, den nativen Windows-Startpfad weiter isolieren
